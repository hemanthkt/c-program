#include <stdio.h>
#include <string.h>

int main()
{
    int n;
    scanf("%d", &n);
    char str[100];
    scanf("%s", str);

    int i = 0;
    while (str[i] != '\0')
    {
        if (strlen(str) < n)
        {
        }
    }
}