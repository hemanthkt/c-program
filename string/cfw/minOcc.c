// Input
// Input string: I love learning programming at Codeforwin.
// Output
// Minimum occurring character is '.' = 1

#include <stdio.h>
#include <string.h>

void minOcc(char str[]) int main()
{
    char str[100];
    scanf("%[^\n]", str);

    minOcc(str);
}

void minOcc(char str[])
{
    int i = 0;
    while (str[i] != '\0')
    {
        y
    }
}