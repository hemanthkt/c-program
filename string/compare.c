#include <stdio.h>
int my_strcomp(const char *str1, const char *str2);

int main()
{
    char str1[100];
    char str2[100];
    scanf("%[^\n]", str1);
    getchar();
    scanf("%[^\n]", str2);

    my_strcomp(str1, str2);
}

int my_strcomp(const char *s1, const char *str2)
{
    while ()
}